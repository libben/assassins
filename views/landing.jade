extends layout
block header
  if game_status === 'on'
    p#announcement_text
    include partials/templates/on_header.jade
  else if game_status === 'off'
    p#countdown_text
    include partials/templates/off_header.jade
block content
  p Howdy! This page will probably have a public kill log updating with sockets soon.
  if game_status === 'over'
    h1= winner_name + ' is the winner!'
